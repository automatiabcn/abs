// `/admin/graph-context` canonical route — re-exports /panel/graph (Growth
// Context Graph). Named graph-context to avoid colliding with /admin/graph
// (the existing Knowledge Graph screen).
export { default } from "@/app/panel/graph/page";
